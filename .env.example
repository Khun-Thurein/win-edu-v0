# Copy to .env.local and fill in values as needed.
# Vite exposes only VITE_* variables to the client bundle.

# Optional: Meta app ID for the Facebook Page Plugin on the Social page.
# VITE_FACEBOOK_APP_ID=

# Optional: Google Form URL for undergraduate consultation (Contact page).
# VITE_UNDERGRAD_FORM_URL=
